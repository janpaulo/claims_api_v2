const config = {
  // host: process.env.HOST,
  // user: process.env.USER,
  // password: process.env.PASSWORD,
  // database: process.env.DATABASE,
  host: "localhost",
  user: "root", // Change this if needed
  password: "xxx123", // Make sure it's correct
  database: "claims_api",
  connectionLimit: 10
};

module.exports = config;
